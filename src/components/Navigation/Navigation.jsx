import React from "react";

const Navigation = () => {
  // one of two navigations will display based on whether we are
  // on dekstop or tablet / mobile
  return <div>responsible for the navigation menu</div>;
};

export default Navigation;
